# A股蜡烛图形态筛选工具 依赖
# 安装：pip install -r requirements.txt
mootdx>=0.10        # 通达信 K线（日/周/月线，TCP 不封IP）
requests>=2.28      # HTTP 数据源（腾讯行情）
pandas>=1.5         # mootdx 依赖 / 数据处理
numpy>=1.23         # 数值计算
flask>=3.0          # Web 服务
